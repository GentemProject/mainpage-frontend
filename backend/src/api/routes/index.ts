/* import { organizations } from './organizations';

const api = {
  organizations: '/organizations',
};

const organizationsApi = (api.organizations, organizations);

export default organizationsApi;
 */
